[Service]
# Keep mutable SQLite data outside the application checkout.  systemd creates
# this directory and assigns it to the User/Group configured by the main unit.
StateDirectory=sn_generator
StateDirectoryMode=0750
Environment=DB_PATH=/var/lib/sn_generator/sn_generator.db

